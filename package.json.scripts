{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --report-unused-disable-directives",
    "format": "prettier --write .",
    "test": "vitest",
    "analyze": "vite build && open dist/stats.html",
    "size": "vite build && ls -lh dist/assets/**/*.{js,css}"
  }
}